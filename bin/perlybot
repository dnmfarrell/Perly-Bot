#!/usr/bin/env perl
use lib 'lib';
use Perly::Bot;
use Perly::Bot::Config;
use Getopt::Long 'GetOptions';

GetOptions(
  'prod' => \my $prod,
);

Perly::Bot::Config->instance(tier => $prod ? 'prod' : 'default');
Perly::Bot->new->run;
